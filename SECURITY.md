# 安全策略

## 支持范围

本项目尚未发布稳定版本。安全修复只针对默认分支的最新代码；旧提交、分支和第三方修改版本不提供安全维护承诺。

## 私下报告漏洞

请使用 GitHub 仓库 **Security** 页面中的 **Report a vulnerability** 私下提交报告。不要在公开 Issue、讨论区或日志中发布漏洞细节、API Key、登录 Cookie、课堂令牌或其他个人信息。

如果仓库尚未启用私密漏洞报告，请通过仓库所有者公开资料中列出的私密联系方式请求安全报告渠道，不要公开披露利用方法。

一份有帮助的报告通常包括：

- 受影响的版本或提交
- 问题类型、影响范围和复现前提
- 最小化复现步骤或概念验证
- 预期行为与实际行为
- 建议修复方向（如有）

请先删除报告中的真实凭据、账号、课程和个人数据。维护者会在可行范围内确认报告、评估影响并协调修复与披露，但不承诺固定响应时限。

## 敏感数据

`config.json` 可能包含模型 API Key 和 SMTP 授权码，`session.json` 包含可恢复登录状态的 Cookie，服务器的 `admin/service.env` 包含管理员密码。这些文件不应提交、分享或附加到 Issue，并应保持仅部署管理员可读。日志也可能含账号名称、课程名称和内部标识，公开前应人工检查并脱敏。

一旦 API Key 或会话凭据泄露，请立即在对应服务撤销或失效处理。修改 Git 历史不能替代凭据轮换。

## 部署边界

本机手动运行默认只监听 `127.0.0.1`。监听非回环地址时，服务会强制要求管理员账号和至少 12 位密码；官方部署脚本默认启用这项保护。

管理员登录不提供传输加密。公网域名必须通过可信的 HTTPS 反向代理访问，并设置安全 Cookie；直接使用 HTTP 只适合受信任的内网。不得绕过管理员认证、同源检查或把管理员凭据交给不受信任的用户。
